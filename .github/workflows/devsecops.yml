name: code-quality

on:
  push:
    branches: [main]

jobs:
  # Job 1: Executes your code quality checks
  code-quality-check:
    uses: ./.github/workflows/code-quality.yml

  # Job 2: Executes your secret scanning checks
  secret-scanning-check:
    uses: ./.github/workflows/secret-scan.yml