name: CI

on:
  push:
    branches: [master]

jobs:
  frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: Verify the code
        run: npm run test